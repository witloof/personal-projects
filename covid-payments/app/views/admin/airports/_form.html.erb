<%= form_with(model: [:admin, admin_airport], local: true) do |form| %>
  <% if admin_airport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_airport.errors.count, "error") %> prohibited this admin_airport from being saved:</h2>

      <ul>
        <% admin_airport.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="form-group col-md-6">
      <%= form.label 'Pays', for: :country_iso_code %>
      <%= form.select(:country_iso_code, countries_options, { :prompt => 'Choisir' }, { class:"form-control" })%>
    </div>
    <div class="form-group col-md-6">
      <%= form.label "Nom", for: :name %>
      <%= form.text_field :name, class:"form-control" %>
    </div>
  </div>
  <div class="actions">
    <%= form.submit button_action, class:"btn btn-primary" %>
  </div>
<% end %>
