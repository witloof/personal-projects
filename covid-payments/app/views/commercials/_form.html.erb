<%= form_with(model: commercial, local: true) do |form| %>
  <% if commercial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commercial.errors.count, "error") %> prohibited this commercial from being saved:</h2>

      <ul>
        <% commercial.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
